/*let isMaintenance = document.getElementById("info-bulle-maintenance").getAttribute("data-maintenance");

 console.log(isMaintenance);
// Fonction pour mettre à jour isMaintenance
function updateMaintenanceStatus(status) {
    status= isMaintenance;
    console.log(status);

    // Mettre à jour l'affichage de la bulle de maintenance si nécessaire
    if (isMaintenance) {
        document.getElementById("info-bulle-maintenance").style.display = "block";
    } else {
        document.getElementById("info-bulle-maintenance").style.display = "none";
    }
}

updateMaintenanceStatus(isMaintenance);
*/

